'use strict';

/**
 * zemne-prace-kontakt router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::zemne-prace-kontakt.zemne-prace-kontakt');
