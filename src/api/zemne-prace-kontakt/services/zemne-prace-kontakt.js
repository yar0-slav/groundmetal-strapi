'use strict';

/**
 * zemne-prace-kontakt service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::zemne-prace-kontakt.zemne-prace-kontakt');
