'use strict';

/**
 * zemne-prace-kontakt controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::zemne-prace-kontakt.zemne-prace-kontakt');
